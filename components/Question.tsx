import React from 'react'
import * as quesdom from "../../types/quesdom";

interface QuestionComponentProps {
    onSubmit?: (any) => any,
    data: quesdom.multipleChoice
}
export const Question = (props: QuestionComponentProps) => {
    return (
        <div>
            
        </div>
    )
}
